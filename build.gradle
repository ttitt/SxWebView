// 根目录 build.gradle 文件
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.4'  // 或使用与您的 Gradle 版本兼容的版本
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
